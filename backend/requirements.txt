# Constellation backend dependencies.

fastapi
uvicorn[standard]
httpx
redis
fakeredis
pydantic
pydantic-settings

# Phase 1 — scraping + recommender
letterboxdpy
scikit-learn
numpy

# Phase 2 — projection
umap-learn
numba>=0.60  # pin a Python 3.12-compatible numba (umap-learn's dep; uv otherwise picks an ancient one)

# Phase 4 — streamed build (SSE)
sse-starlette

# Dev / test
pytest
pytest-asyncio
ruff
