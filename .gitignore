# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# uv writes an empty lock here because deps live in backend/requirements.txt
# (the manifest, per DECISIONS.md), not in pyproject [project].
uv.lock

# Env / secrets — NEVER commit real keys
.env
.env.local
*.local

# Generated per-user artifacts (personal taste data; keep local)
recommendations-*.md
graph-*.json

# Frontend (added in later phases)
node_modules/
.next/
dist/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
